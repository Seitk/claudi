## IDF Component Manager Manifest File
# Board-specific managed dependencies (e.g. the Waveshare AMOLED BSP) live in the
# selected claudi_board_* impl's idf_component.yml, not here, so that building for
# a different board does not pull another board's BSP.
